"""
Estruturas lógicas: and(e), or(ou), not(não), is(é)

Operadores unários:
    - not
Operadores binários:
    - and, or, is

Regras de funcionamento:
Para o and, ambos os valores precisam ser True para retornar True;
Para o or, pelo menos um dos valores precisam ser True para retornar True;
Para o not, o valor do booleano é invertido, se for True vira False e vice versa;
Para o is, o valor booleano é testado, ele compara os valores;
"""